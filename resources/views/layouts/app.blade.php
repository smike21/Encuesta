<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title', 'Página de Encuestas')</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/style.css') }}">@stack('styles')</head><body><nav class="navbar"><div class="container py-2"><a class="navbar-brand" href="{{ route('surveys.index') }}">Página de Encuestas</a><div>@auth @if(auth()->user()->is_admin)<a href="{{ route('admin.dashboard') }}">Panel</a>@endif <form class="d-inline" method="post" action="{{ route('admin.logout') }}">@csrf <button class="link-button">Salir</button></form>@else <a href="{{ route('admin.login') }}">Administración</a>@endauth</div></div></nav><main class="container py-5">@foreach(['success','warning'] as $type) @if(session($type))<div class="alert alert-{{ $type }}">{{ session($type) }}</div>@endif @endforeach @yield('content')</main><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>@stack('scripts')</body></html>
