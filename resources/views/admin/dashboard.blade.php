@extends('layouts.app') @section('title','Panel de administración') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1>Panel de administración</h1><p>Administra tus encuestas.</p></div><a class="btn btn-primary" href="{{ route('admin.create') }}">Nueva encuesta</a></div><div class="row g-4">@forelse($surveys as $survey)<div class="col-md-6 col-lg-4"><article class="card h-100"><div class="card-body"><h2 class="h5">{{ $survey->title }}</h2><p>{{ $survey->description }}</p><span class="badge text-bg-{{ $survey->is_active ? 'success':'secondary' }}">{{ $survey->is_active ? 'Activa':'Inactiva' }}</span><p class="small mt-3 mb-0">{{ $survey->questions_count }} preguntas · {{ $survey->submissions_count }} respuestas</p></div><div class="card-footer d-flex flex-wrap gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.results',$survey) }}">Resultados</a><form method="post" action="{{ route('admin.toggle',$survey) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-warning">{{ $survey->is_active ? 'Desactivar':'Activar' }}</button></form><form method="post" action="{{ route('admin.destroy',$survey) }}" onsubmit="return confirm('¿Eliminar esta encuesta?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Eliminar</button></form></div></article></div>@empty <p class="text-muted">Aún no hay encuestas.</p>@endforelse</div>@endsection
