@extends('layouts.app') @section('title','Resultados') @section('content')<a href="{{ route('admin.dashboard') }}">← Volver</a><h1 class="mt-3">{{ $survey->title }}</h1><p>{{ $survey->description }}</p><div class="row g-3 mb-4"><div class="col-md-4"><div class="card card-body"><strong>{{ $survey->submissions->count() }}</strong> respuestas</div></div><div class="col-md-4"><div class="card card-body"><strong>{{ $survey->questions->count() }}</strong> preguntas</div></div><div class="col-md-4"><div class="card card-body"><strong>{{ $survey->submissions->whereNotNull('latitude')->count() }}</strong> ubicaciones</div></div></div><h2 class="h4">Respuestas por pregunta</h2>@foreach($survey->questions as $question)<section class="card mb-3"><div class="card-header">{{ $question->text }}</div><ul class="list-group list-group-flush">@forelse($question->answers as $answer)<li class="list-group-item">{{ $answer->value }} <small class="text-muted">{{ $answer->created_at->format('d/m H:i') }}</small></li>@empty<li class="list-group-item text-muted">Sin respuestas aún</li>@endforelse</ul></section>@endforeach<h2 class="h4 mt-4">Ubicaciones</h2><ul class="list-group">@forelse($survey->submissions as $submission)<li class="list-group-item">{{ $submission->latitude !== null ? $submission->latitude.', '.$submission->longitude : 'No disponible' }} @if($submission->latitude !== null)<a target="_blank" href="https://www.google.com/maps/search/?api=1&query={{ $submission->latitude }},{{ $submission->longitude }}">Ver mapa</a>@endif</li>@empty<li class="list-group-item text-muted">Sin respuestas</li>@endforelse</ul>@endsection
